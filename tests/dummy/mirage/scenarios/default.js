export default function (server) {
  server.createList("scope", 50);
  server.createList("user", 50);
  server.createList("permission", 50);
  server.createList("role", 50);
  server.createList("acl", 50);
}
